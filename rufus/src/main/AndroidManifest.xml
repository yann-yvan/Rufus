<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="corp.ny.com.rufus">

    <application android:name=".system.RufusApp" >
        <provider
            android:name=".system.RufusInitProvider"
            android:authorities="${applicationId}.rufusinitprovider"
            android:exported="false"
            android:initOrder="100" />
    </application>

</manifest>
